# Phase 1 is intentionally incapable of signing or submitting.
dry_run: true
rpc_url: https://solana-rpc.publicnode.com
commitment: finalized

# Derived from the Wallet A diagnostic.
minimum_buy_usd: 300
maximum_transaction_gap: 3
sol_price_usd: 73.385944
replay_fixture: fixtures/wallet_a_replay.json

# Wallet A sample economics. The reserve amortizes its observed failed-attempt
# fees over each eventual success; the net floor is its smallest observed win.
failed_attempt_reserve_lamports: 815123
minimum_net_profit_lamports: 279929

# Read-only Jupiter /build + Solana simulateTransaction settings. This public
# key is used only as simulation account state; no private key is loaded.
shadow_taker: MRiYA4oN3158fCV8evhuCofrDzbHyYvYnGZUDJvoCsa
shadow_input_lamports: 10000000
slippage_bps: 100
max_accounts: 50
jito_tip_lamports: 100000
safety_margin_lamports: 100000

# Direct Wallet-A-style Pump AMM <-> Meteora DLMM shadow execution. The
# executor ID and route-compatible ALT remain deliberately unset until the SBF
# artifact is independently reviewed and deployed. A blank ID keeps the CLI
# fail-closed. The gross floor covers failed-attempt reserve, desired wallet-net
# profit, safety margin, maximum landed fee, and any configured direct tip.
executor_program_id: ""
direct_lookup_table_addresses: []
direct_compute_unit_limit: 600000
direct_compute_unit_price_micro_lamports: 800000
direct_opportunity_slot_ttl: 2
maximum_transaction_fee_lamports: 700000
direct_tip_lamports: 0
direct_tip_recipient:

# $100 dedicated-wallet risk policy. Live remains locked until the executor,
# profit guard, and shadow proof flags in runtime state are all verified.
risk_state_path: data/runtime-state.json
pilot_risk_pct: 5
validated_risk_pct: 10
trade_cap_usd: 25
wallet_reserve_usd: 10
daily_loss_pct: 3
maximum_drawdown_pct: 5
